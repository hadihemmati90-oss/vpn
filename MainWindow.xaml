<Window x:Class="XrayClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Xray Client" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16">
            <DockPanel>
                <TextBlock VerticalAlignment="Center" Margin="16 0 0 0" Text="Xray Client - Secure VPN" FontSize="24" />
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <ToggleButton Name="ModeToggle" IsChecked="False" Margin="10" Style="{StaticResource MaterialDesignSwitchToggleButton}" ToolTip="Enable TUN Mode" />
                    <TextBlock Text="TUN Mode" VerticalAlignment="Center" Margin="5"/>
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- Main Dashboard -->
        <Grid Grid.Row="1" Margin="20">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Name="StatusText" Text="Disconnected" Style="{StaticResource MaterialDesignHeadline3TextBlock}" HorizontalAlignment="Center" Foreground="Gray"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 20">
                    <materialDesign:Card Margin="10" Padding="10" Width="150" UniformCornerRadius="10">
                        <StackPanel>
                            <TextBlock Text="Upload" FontWeight="Bold"/>
                            <TextBlock Name="UploadSpeed" Text="0 KB/s" FontSize="18"/>
                        </StackPanel>
                    </materialDesign:Card>
                     <materialDesign:Card Margin="10" Padding="10" Width="150" UniformCornerRadius="10">
                        <StackPanel>
                            <TextBlock Text="Download" FontWeight="Bold"/>
                            <TextBlock Name="DownloadSpeed" Text="0 KB/s" FontSize="18"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>

                <Button Name="ConnectBtn" Content="CONNECT" Width="200" Height="50" FontSize="18" Click="ConnectBtn_Click" Style="{StaticResource MaterialDesignRaisedButton}"/>
            </StackPanel>
        </Grid>

        <!-- Logs -->
        <materialDesign:Card Grid.Row="2" Margin="10" VerticalAlignment="Stretch" Background="#222">
            <ScrollViewer Name="LogScroll">
                <TextBox Name="LogBox" IsReadOnly="True" BorderThickness="0" Background="Transparent" Foreground="#ddd" FontFamily="Consolas" TextWrapping="Wrap" Margin="5"/>
            </ScrollViewer>
        </materialDesign:Card>
    </Grid>
</Window>
